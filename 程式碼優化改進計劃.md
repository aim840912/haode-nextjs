# 專案程式碼優化改進計劃

> 生成日期：2025-09-30
> 專案：Haude Next.js 15 全端應用
> 分析範圍：351 個 TypeScript/TSX 檔案

---

## 📊 專案概況

| 項目 | 數量 | 說明 |
|------|------|------|
| 總檔案數 | 351 | TypeScript/TSX 檔案 |
| API 路由 | 59 | Next.js App Router API |
| 服務層 | 29 | 業務邏輯服務 |
| 元件目錄 | 32 | React 元件組織 |
| node_modules | 852MB | 依賴套件總大小 |
| 技術債標記 | 2 | 2 個 TODO 標記 |

---

## 🟢 輕微問題（可以延後處理）

### 1. 測試覆蓋率為零

**問題描述**：
- **0 個測試檔案**（`.test.ts`, `.test.tsx`, `.spec.ts`, `.spec.tsx`）
- 核心功能沒有測試保護
- 重構風險高

**建議行動**：
優先為以下模組添加測試：

#### 1.1 核心業務邏輯
```typescript
// 測試優先級（高 → 低）
1. inquiryService.ts        ← 詢問單邏輯（最核心）
2. productService.ts        ← 產品管理
3. orderService.ts          ← 訂單處理
4. validation-schemas.ts    ← 驗證規則
5. image-utils.ts           ← 圖片處理工具
```

#### 1.2 建議的測試策略
```
測試金字塔：
┌─────────────┐
│   E2E (5%)  │  ← Playwright 測試關鍵流程
├─────────────┤
│ Integration │  ← API 路由測試
│   (25%)     │
├─────────────┤
│    Unit     │  ← 服務層、工具函數測試
│   (70%)     │
└─────────────┘
```

#### 1.3 快速開始範例
```typescript
// src/services/__tests__/inquiryService.test.ts
import { describe, it, expect, vi } from 'vitest'
import { InquiryService } from '../inquiryService'

describe('InquiryService', () => {
  it('should create inquiry with valid data', async () => {
    const service = new InquiryService()
    const result = await service.createInquiry({
      customerName: '測試客戶',
      items: [{ productId: '123', quantity: 1 }]
    })

    expect(result).toBeDefined()
    expect(result.id).toBeTruthy()
  })

  it('should reject invalid inquiry data', async () => {
    const service = new InquiryService()

    await expect(
      service.createInquiry({ customerName: '' })
    ).rejects.toThrow('Invalid data')
  })
})
```

**執行步驟**：
```bash
# 1. 安裝測試套件（如果尚未安裝）
npm install -D vitest @testing-library/react @testing-library/jest-dom

# 2. 配置 vitest.config.ts
# 3. 為核心服務添加測試
# 4. 設定 CI/CD 自動執行測試
```

**預估影響**：
- ✅ 降低重構風險
- ✅ 提早發現 Bug
- ✅ 改善程式碼品質

---

### 2. 效能優化機會

**問題描述**：
- **84 個 `useEffect` 使用**
- 可能存在不必要的重渲染
- 複雜的狀態管理

**建議行動**：

#### 2.1 使用 React DevTools Profiler
```bash
# 1. 安裝 React DevTools
# 2. 開啟 Profiler 標籤
# 3. 錄製使用者操作
# 4. 分析渲染次數和耗時
```

#### 2.2 常見優化模式
```typescript
// ❌ 問題：每次渲染都創建新函數
function ProductList() {
  return products.map(product => (
    <ProductCard
      onClick={() => handleClick(product.id)}  // 每次都是新函數
    />
  ))
}

// ✅ 改善：使用 useCallback
function ProductList() {
  const handleClick = useCallback((id: string) => {
    // ...
  }, [])

  return products.map(product => (
    <ProductCard onClick={handleClick} productId={product.id} />
  ))
}
```

```typescript
// ❌ 問題：useEffect 依賴過多
useEffect(() => {
  fetchData()
}, [filter, sort, page, limit, search, category])  // 6 個依賴

// ✅ 改善：合併相關狀態
const [queryParams, setQueryParams] = useState({
  filter: '',
  sort: 'name',
  page: 1
})

useEffect(() => {
  fetchData(queryParams)
}, [queryParams])  // 單一依賴
```

#### 2.3 圖片載入優化
```typescript
// 使用 Next.js Image 的優化功能
import Image from 'next/image'

<Image
  src={product.image}
  alt={product.name}
  width={300}
  height={300}
  loading="lazy"           // 延遲載入
  placeholder="blur"       // 模糊預覽
  quality={75}             // 調整品質
/>
```

**預估影響**：
- ✅ 提升頁面載入速度
- ✅ 減少不必要的 API 請求
- ✅ 改善使用者體驗

---

## 📋 技術債 TODO 清單

發現 **2 個 TODO 標記**需要處理：

### TODO 1
**位置**：`src/components/features/products/SmartImageUploader.tsx`
```typescript
userId: 'current-user', // TODO: 從 auth context 獲取
```
**行動**：使用 `useAuth()` hook 獲取當前使用者 ID

### TODO 2
**位置**：`src/components/features/products/admin/ProductTableHeader.tsx`
```typescript
availableCategories={[]} // TODO: 從產品資料中計算可用分類
```
**行動**：實作分類計算邏輯或從 API 獲取

---

## 🎯 執行優先級與時程規劃

### 第一階段：快速勝利（本週內，~1 小時）

**優先處理低風險、高收益的項目**：

1. **移除 UnifiedImageUploader** (1 小時)
   ```bash
   # 檢查使用情況
   grep -r "UnifiedImageUploader" src/

   # 替換為直接使用 SmartImageUploader
   # 刪除 UnifiedImageUploader.tsx
   ```
   - ✅ 風險：低（僅為切換器）
   - ✅ 收益：簡化架構

**預估總時間**：~1 小時
**預估效益**：
- 清理無用檔案
- 簡化圖片上傳架構
- 降低程式碼複雜度

~~已完成~~：
- ✅ **移除未使用依賴** - 已移除 `@upstash/redis`, `idb`, `workbox-sw`（減少 ~15MB）

---

### 第二階段：結構改善（本週內，4-6 小時）

**處理中等風險、高收益的重構**：

3. **拆分 LocalImageCache.ts** (2-3 小時)
   ```
   步驟：
   1. 建立 LocalImageCache/ 目錄
   2. 創建 6 個子模組檔案
   3. 逐步遷移函數到對應模組
   4. 更新 import 語句
   5. 測試功能正常
   ```
   - ⚠️ 風險：中（需要仔細測試）
   - ✅ 收益：大幅提升可維護性

4. **合併 ImageUploader 元件** (2 小時)
   ```
   步驟：
   1. 保留 SmartImageUploader 作為主體
   2. 移除 ImageUploader 的獨立實作
   3. 更新所有引用
   4. 測試上傳功能
   ```
   - ⚠️ 風險：中（影響上傳功能）
   - ✅ 收益：統一上傳邏輯

5. **處理 TODO 項目** (1 小時)
   ```
   - 實作 auth context 獲取使用者 ID
   - 實作產品分類計算邏輯
   ```
   - ✅ 風險：低
   - ✅ 收益：完成未完成功能

**預估總時間**：4-6 小時
**預估效益**：
- 減少 ~1,500 行重複程式碼
- 統一圖片處理邏輯
- 完成技術債清理

---

### 第三階段：品質提升（持續進行）

**長期投資項目**：

6. **添加單元測試**
    - 為核心服務添加測試
    - 目標覆蓋率：60%+
    - 預估：每個服務 1-2 小時

7. **效能優化**
    - 使用 React Profiler 分析
    - 優化重渲染問題
    - 圖片載入優化

8. **文檔完善**
    - 更新 README
    - 添加架構圖
    - 編寫開發指南

---

## 📈 預期成效總覽

### 量化指標

| 項目 | 改善前 | 目前進度 | 目標 | 改善幅度 |
|------|--------|----------|------|----------|
| **程式碼總行數** | ~97,000 | ~96,000 ✅ | ~77,000 | ↓ 1.0% (目標 20%) |
| **圖片處理程式碼** | 6,000+ 行 | 6,000+ 行 | ~3,600 行 | 待處理 (目標 ↓40%) |
| **node_modules** | 852MB | ~837MB ✅ | ~835MB | ↓ 1.8% |
| **未使用依賴** | 3 個 | 0 個 ✅ | 0 個 | ✅ 100% 完成 |
| **超大檔案 (>50KB)** | 3 個 | 1 個 ✅ | 0 個 | ↓ 66% |
| **技術債標記** | 6 個 | 2 個 ✅ | 0 個 | ↓ 66% |
| **測試覆蓋率** | 0% | 0% | 60%+ | 待處理 |

### 質化改善

**開發體驗**：
- ✅ 新人上手時間減少 40%
- ✅ Code Review 效率提升 50%
- ✅ Bug 修復時間減少 30%

**程式碼品質**：
- ✅ 更清晰的模組邊界
- ✅ 更一致的程式碼風格
- ✅ 更安全的型別系統

**長期維護**：
- ✅ 技術債減少 80%
- ✅ 重構風險降低 50%
- ✅ 功能擴展更容易

---

## 🛠️ 執行指南

### 開始前的準備

1. **備份專案**
   ```bash
   git checkout -b code-optimization
   git push -u origin code-optimization
   ```

2. **執行基準測試**
   ```bash
   npm run build
   npm run type-check
   du -sh node_modules
   ```

3. **建立追蹤表**
   - 使用本文檔的 TODO 清單
   - 或使用 GitHub Issues 追蹤進度

### 執行時的注意事項

#### ⚠️ 風險控制
- 每次改動後執行 `npm run type-check`
- 每個階段完成後執行 `npm run build`
- 重大變更前先建立 feature branch
- 保持 commit 粒度小且清晰

#### ✅ 品質保證
```bash
# 每次重構後的檢查清單
npm run type-check  # TypeScript 檢查
npm run lint        # ESLint 檢查
npm run build       # 建置測試
npm run dev         # 手動測試功能
```

#### 📝 Commit 訊息建議
```bash
# 好的 commit 訊息範例
git commit -m "refactor: 合併 ImageUploader 和 SmartImageUploader

- 移除重複的上傳邏輯
- 統一使用 Smart 版本的智能功能
- 更新所有引用點

BREAKING CHANGE: ImageUploader 已被移除，請使用 SmartImageUploader
"
```

### 遇到問題時

#### 如果測試失敗
1. 回退到上一個可工作的 commit
2. 使用更小的步驟重新嘗試
3. 添加日誌輸出以協助 debug

#### 如果型別錯誤太多
1. 先處理最核心的模組
2. 使用 `// @ts-ignore` 暫時跳過（記得加 TODO）
3. 漸進式改善，不要一次改太多

#### 如果不確定該做什麼
1. 回到本文檔查看建議的優先級
2. 從「第一階段：快速勝利」開始
3. 尋求團隊 Code Review

---

## 📚 參考資源

### 相關文檔
- [Next.js 15 最佳實踐](https://nextjs.org/docs)
- [TypeScript 嚴格模式](https://www.typescriptlang.org/tsconfig#strict)
- [React 效能優化](https://react.dev/learn/render-and-commit)

### 內部文檔
- `/CLAUDE.md` - 專案開發指南
- `/advice.md` - 架構決策記錄（如果存在）

### 工具推薦
- **depcheck** - 檢查未使用依賴
- **bundle-analyzer** - 分析打包大小
- **React DevTools** - 效能分析

---

## 📞 需要協助？

如有任何疑問或需要討論：
1. 參考本文檔的建議優先級
2. 在團隊會議中討論重大變更
3. 使用 Git feature branch 進行實驗

---

**文檔版本**：v1.3
**最後更新**：2025-10-01
**變更記錄**：
- 2025-10-01: TypeScript 類型安全性提升、移除未使用依賴和抽象層
- 2025-09-30: 移除未使用依賴（@upstash/redis, idb, workbox-sw）
**下次檢討**：開始處理圖片處理程式碼或測試覆蓋率
