# 產品建立功能測試說明

## 問題診斷

在測試過程中發現以下問題：

### 問題 1: Zod Schema 驗證錯誤 ✅ 已修復
- **錯誤**: `Invalid input: expected number, received null`
- **原因**: Zod schema 不接受 `null` 值給 `originalPrice` 和 `saleEndDate`
- **修復**: 在 Zod schema 中新增 `.nullable()` 修飾符
- **檔案**: `src/app/api/admin/products/create-with-images/route.ts:22-23`

### 問題 2: PostgreSQL 函數欄位不存在 ✅ 已完全修復
- **錯誤**: 多個欄位不存在
  - `column "sku" of relation "products" does not exist`
  - `column "is_on_sale" of relation "products" does not exist`
  - `column "original_price" of relation "products" does not exist`
  - `column "sale_end_date" of relation "products" does not exist`
- **原因**: RPC 函數試圖插入資料庫中不存在的欄位
- **修復**: 建立完整修復腳本，移除所有不存在的欄位引用
- **檔案**: `scripts/fix-complete-schema-mismatch.sql`

## 修復步驟

### 1. 執行資料庫修復腳本

請在 **Supabase Dashboard > SQL Editor** 中執行以下腳本：

```bash
scripts/fix-complete-schema-mismatch.sql
```

這個腳本會：
- ✅ 更新 `create_product_with_images` 函數
- ✅ 移除所有不存在欄位的引用（sku, is_on_sale, original_price, sale_end_date）
- ✅ 只保留實際存在的欄位
- ✅ 保持完整的錯誤處理機制

### 2. 重新測試產品建立

執行腳本後，重新嘗試建立產品：

1. 開啟瀏覽器到 http://localhost:3000/admin/products/add
2. 填寫產品資訊：
   - 名稱：高山烏龍茶
   - 描述：來自海拔1000公尺的梅山高山烏龍茶...
   - 分類：茶葉
   - 價格：800
   - 庫存：50
3. 上傳圖片：`/mnt/c/Users/aim84/Downloads/HAUDE/images/products/oolong-tea.jpg`
4. 點擊「建立並上架產品」

### 3. 預期結果

✅ **成功情境**：
- 產品成功建立
- 圖片成功關聯
- 頁面重導向到產品列表
- 產品顯示在列表中

❌ **如果仍然失敗**：
- 檢查 Supabase 是否已成功執行修復腳本
- 檢查伺服器日誌中的錯誤訊息
- 確認資料庫 `products` 表格確實沒有 `sku` 欄位

## 架構改進建議

這個問題突顯了三層架構的同步重要性：

1. **API Layer (TypeScript)**: Zod Schema 驗證
2. **Database Layer (PostgreSQL)**: RPC 函數邏輯
3. **Storage Layer (PostgreSQL)**: 實際表格結構

**最佳實踐**：
- 當資料庫 schema 變更時，同步更新：
  - RPC 函數定義
  - API 路由 Zod schema
  - TypeScript 型別定義
- 使用 migration 腳本管理資料庫變更
- 在開發環境中先測試完整流程

## 已修復的檔案

1. ✅ `src/app/api/admin/products/create-with-images/route.ts`
   - 移除 Zod schema 中的所有不存在欄位
   - 移除：`sku`, `isOnSale`, `originalPrice`, `saleEndDate`
   - 只保留實際存在的欄位

2. ✅ `scripts/fix-complete-schema-mismatch.sql`
   - **需要在 Supabase 中執行**
   - 更新 RPC 函數移除所有不存在的欄位
   - 完全對齊資料庫實際結構

## 測試檢查清單

- [ ] 執行 `fix-complete-schema-mismatch.sql` 腳本
- [ ] 重新整理頁面
- [ ] 填寫產品表單
- [ ] 上傳產品圖片
- [ ] 提交表單
- [ ] 確認產品成功建立
- [ ] 確認圖片成功關聯
- [ ] 檢查產品列表頁面

## 技術債清理建議

1. **✅ 已完成：移除資料庫中所有不存在欄位的引用**
   - ✅ 更新主要 RPC 函數 `create_product_with_images`
   - ⚠️ 檢查其他 RPC 函數（如全文搜尋函數）
   - ⚠️ 檢查其他 API 路由
   - ⚠️ 更新 TypeScript 型別定義

2. **統一欄位命名規範**
   - 決定是使用 `inventory` 還是 `stock` (目前 DB 用 `stock`，API 用 `inventory`)
   - 決定是使用 `priceUnit` 還是 `price_unit` (目前混用)
   - 在所有三層中統一命名

3. **建立自動化測試**
   - E2E 測試產品建立流程
   - 單元測試 RPC 函數
   - 整合測試 API 路由

4. **檢查全文搜尋函數**
   - `scripts/full-text-search-functions.sql` 也引用了不存在的欄位
   - 需要同步更新以避免搜尋功能出錯